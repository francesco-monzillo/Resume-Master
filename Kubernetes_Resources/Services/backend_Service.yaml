apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  type: ClusterIP # Internal only. Change to LoadBalancer if SPAs need direct external access.
  ports:
  - port: 80
    targetPort: 8000
  selector:
    app: backend